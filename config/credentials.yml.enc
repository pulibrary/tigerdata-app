# The default Rails way to manage secrets is with this credentials file
# that *is* checked in, and a master.key that is gitignored.
#
# However, at PUL we instead use environment variables configured in princeton_ansible, which are reference in config files via the rails ENV[:blah].