# The default Rails way to manage secrets is with this credentials file
# that *is* checked in, and a master.key that is gitignored.
#
# However, at PUL we instead use environment variables, and gitignored config files.