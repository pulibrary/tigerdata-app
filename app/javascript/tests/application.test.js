import { assert, describe, expect, it } from 'vitest'
// import { initPage } from '../entrypoints/application';

describe('TigerData', () => {
  it('can test JS', () => {
    assert.equal(2+2, 4)
  });
});