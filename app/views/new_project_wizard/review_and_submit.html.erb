<h2>Review and Submit</h2>
<%= form_with(url: new_project_review_and_submit_save_path(@request_model), model: @request_model, action: :save, method: :put) do |form| %>
  <%= @request_model.id %>
  <div>
    <%= form.label :project_title %>
    <%= form.text_field :project_title %>
  </div>
  <%= render partial: '/new_project_wizard/navigation', locals: {form:} %>
<% end %>
