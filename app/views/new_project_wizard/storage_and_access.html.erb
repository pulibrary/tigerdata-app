<% content_for :title, "New Project Request" %>
<% content_for :form_title, "Storage and Access" %>
<% content_for :form_description, "Enter the storage and access needs for your project. You can always request changes later if needed." %>

<%= render partial: "/new_project_wizard/form_project_storage", locals: { quota: @new_project_request.quota || "", quota_error: "", 
                                                                          storage_size: @new_project_request.storage_size || 0,  storage_size_error: "", 
                                                                          storage_unit: @new_project_request.storage_unit || "GB",  storage_unit_error: "" } %>
