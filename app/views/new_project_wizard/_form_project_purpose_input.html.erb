<div class="form-field project-purpose">
  <%= render partial: "/new_project_wizard/form_field_label",
           locals: {field_label: "Project Purpose", field_sub_label: "Required"} %>
  <div class="input-frame">
    <div class="text-input">
      <div class="horizontal-frame">
        <div class="vertical-frame">
          <project-purpose
            :default-value="<%= project_purpose.to_json %>"
            :options="<%= ProjectPurpose.to_json %>"
            >
            <template v-slot:hidden-input="{ selectedItem }">
              <input type="hidden" name="request[project_purpose]" :value="selectedItem" class="selected-item"></input>
            </template>

          </project-purpose>

          <div class="horizontal-frame error-and-count">
            <div class="input-error">
                <%= project_purpose_error %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>